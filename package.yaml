name:                programming-in-haskell
version:             0.1.0.0

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  programming-in-haskell:
    main:                Main.hs
    source-dirs:         app
